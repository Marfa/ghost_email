#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not found. Install: brew install gitleaks" >&2
  echo "Or skip locally (not recommended): git commit --no-verify" >&2
  exit 1
fi

gitleaks protect --staged --verbose
